name: Run

on:
  schedule:
    - cron: '1 0 * * *' # Run every day at 1:00 am UTC (9:00 pm EST)
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (build + validate, publish nothing)'
        type: boolean
        default: true

permissions:
  contents: write  # push the tsei-storage.json commit
  id-token: write  # OIDC trusted publishing to npm

jobs:
  run:
    runs-on: ubuntu-latest
    env:
      # present only while bootstrapping / using token auth; empty once on OIDC
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'node' # latest; satisfies trusted-publishing Node >=22.14 requirement

      - name: Ensure npm supports trusted publishing
        run: npm install -g npm@latest # OIDC trusted publishing requires npm >=11.5.1

      - name: Configure npm auth (token fallback; skipped when publishing via OIDC)
        if: ${{ env.NPM_TOKEN != '' }}
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ./package-files/.npmrc

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            yarn-

      - name: Install Packages
        run: yarn install --frozen-lockfile

      - name: Configure git
        run: |
          git config user.name "GitHub Actions"
          git config user.email "github-actions@github.com"
          git remote set-url origin https://${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git

      - name: Run
        run: yarn run run
        env:
          CI: true
          NODE_ENV: debug
          DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }} # scheduled runs publish; manual defaults to dry
